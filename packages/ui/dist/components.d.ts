export interface ButtonProps {
    children: string;
    onClick?: () => void;
}
export declare const Button: (props: ButtonProps) => string;
export interface CardProps {
    children: string;
    title?: string;
}
export declare const Card: (props: CardProps) => string;
//# sourceMappingURL=components.d.ts.map